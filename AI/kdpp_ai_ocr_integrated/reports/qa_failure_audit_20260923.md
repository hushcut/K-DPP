# 실사진 QA 실패 원인 감사 — 2026-09-23

## 범위와 재현 기준

- 기준 코드: `kyh/ai`의 `219cb77`. 기존 성공 오답 2건과 인식 실패 29건을 분석했다.
- 기준 자료: 루트 `.test-artifacts/vision-qa-20260923/`의 `real89_results.csv`, `real89_answer_key.csv`, `new_vision_cache.json`.
- 원본·기본 전처리의 일반 텍스트와 좌표 재구성 텍스트를 확인했다. 반사 후보는 기본 평가와 분리했다.
- 사진 직접 대조: QA001, QA003, QA031. 나머지는 저장된 OCR 증거에 대한 분류이며 사진 전체 재주석이나 Vision 단독 오류 판정이 아니다.
- 이 문서는 원문 OCR·사진·개인정보를 포함하지 않는다. 정답지와 기존 캐시는 수정하지 않았다.
- `파서 해석`은 충분한 조성 근거가 텍스트에 있지만 현재 규칙이 처리하지 못한 경우다. `OCR 근거 부족`은 필요한 소재나 비율이 누락·손상된 경우이며 촬영·Vision·배치 복원 중 어느 단계의 책임인지는 추가 실험이 필요하다.
- 분류는 수동 감사 결과다. 정답지를 파서 입력에 넣거나, 정답으로 누락 소재·비율을 보충하지 않았다.

## 성공 판정 불일치 2건

| 사례 | 확인 결과 | 조치 |
| --- | --- | --- |
| QA003 | 사진은 겉감 면 100%, OCR·파서도 면 100%. 정답지는 폴리에스터 95·폴리우레탄 5로 잘못 기록됨 | 파서 동작 유지, 최소 조성 테스트로 보호. 정답지는 원본을 보존하고 재주석 대상으로 기록 |
| QA031 | 사진의 겉감은 면 100%, 배색은 면 95·폴리우레탄 5. OCR이 겉감 소재·부위를 누락하고 배색 마커도 손상시킴. 파서는 남은 독립 100%를 무시하고 배색을 일반 조성으로 성공 반환 | 미연결 독립 비율이 있는 부위를 확정하지 않도록 수정. 원본·좌표 재구성 형태와 유사 조합 회귀 테스트 추가 |

기존 `2/60(3.3%)`는 **정답지 대비 성공 불일치율**이다. 사진 감사 결과 하나는 정답지 오류이고 하나는 실제 대표 부위 오답이었다. 전체 성공 사진을 재주석하지 않았으므로 실제 오답률이 3.3% 또는 0%라고 단정하지 않는다.

## 인식 실패 29건

| 분류 | 건수 |
| --- | ---: |
| 파서 해석 | 11 |
| OCR 근거 부족 | 15 |
| 복합 | 2 |
| 정답지 불일치 우선 | 1 |
| 합계 | 29 |

| 사례 | 분류 | 근거 및 후속 방향 |
| --- | --- | --- |
| QA001 | 정답지 불일치 | 정답지는 면 80·폴리에스터 20이나 사진의 조성은 다름. 캐시도 겉감·안감 일부를 누락함. 대표 부위 재주석 우선. |
| QA002 | 파서 해석 | 면 100%와 다국어 면 별칭 반복이 있음. 비율 없는 번역 행을 미완성 소재로 판단함. |
| QA015 | OCR 근거 부족 | 정답 비율 94·6 중 94가 손상되어 유효 숫자가 2·6으로 추출됨. |
| QA017 | OCR 근거 부족 | 100%는 있으나 면 소재명이 다른 한자로 손상됨. |
| QA020 | 파서 해석 | 면과 100%가 모두 있지만 비율이 소재보다 앞에 있어 조합하지 못함. |
| QA027 | OCR 근거 부족 | 면·겉감 표기가 손상되어 100%에 대응하는 소재를 확인하지 못함. |
| QA037 | OCR 근거 부족 | 면·폴리에스터는 있으나 두 번째 비율 20이 누락됨. |
| QA040 | OCR 근거 부족 | 면 소재는 일부 남아 있으나 완전한 100% 비율 근거가 없음. |
| QA041 | OCR 근거 부족 | 면·나일론은 있지만 정답의 50·50 비율을 읽지 못함. |
| QA042 | OCR 근거 부족 | 100%는 있으나 소재명이 손상됨. |
| QA044 | 파서 해석 | 면·폴리에스터와 80·20이 모두 있지만 비율 선행·혼합 행 순서를 처리하지 못함. |
| QA046 | 복합 | 두 면 표기가 합쳐지고 부위 마커가 연속 배치됨. OCR 행 배치와 소재·부위 해석 모두 관여함. |
| QA052 | OCR 근거 부족 | 60·40과 폴리에스터는 있으나 겉감의 면 소재가 누락됨. |
| QA053 | 파서 해석 | PIMACOTTON 100%를 미등록 소재로 처리함. |
| QA054 | 파서 해석 | PIMACOTTON 100%를 미등록 소재로 처리함. |
| QA055 | OCR 근거 부족 | 면 소재와 비율 일부가 손상됨. 기본 전처리에도 대표 조성 근거가 불충분함. |
| QA057 | OCR 근거 부족 | 겉감의 면 소재가 누락되고 하위 부위 조성에도 숫자 손상이 있음. |
| QA058 | OCR 근거 부족 | 비율 일부만 남고 대표 부위 소재명이 손상됨. |
| QA060 | OCR 근거 부족 | 기본 원본·전처리에는 100%의 소재가 없음. 별도 저장된 반사 후보에는 면 100%가 있으나 이번 기본 평가에는 포함하지 않음. |
| QA061 | 파서 해석 | PIMACOTTON 100%를 미등록 소재로 처리함. |
| QA062 | 파서 해석 | PIMACOTTON 100%를 미등록 소재로 처리함. |
| QA063 | 파서 해석 | PIMACOTTON 100%를 미등록 소재로 처리함. |
| QA073 | OCR 근거 부족 | 면과 100% 문자열이 함께 손상되어 완전한 조성 근거가 없음. |
| QA077 | OCR 근거 부족 | 조성 텍스트가 거의 남지 않음. |
| QA083 | OCR 근거 부족 | 겉감 소재가 누락됨. 배색 95·5가 있어도 겉감 대신 반환하지 않는 정상적인 안전 거절임. |
| QA094 | 파서 해석 | 면 100% 뒤의 다국어 면 별칭 반복을 미완성 소재 행으로 판단함. |
| QA095 | 파서 해석 | 면 100% 뒤의 다국어 면 별칭 반복을 미완성 소재 행으로 판단함. |
| QA097 | 복합 | 면 100%인 완전한 번역 행과 비율 누락·800%로 손상된 번역 행이 공존함. 반복 행 해석과 OCR 손상이 함께 관여함. |
| QA098 | 파서 해석 | 면 100% 뒤의 다국어 면 별칭 반복을 미완성 소재 행으로 판단함. |

## 수정과 부작용 확인

- 기존 소재 행 검증에 더해, 완전한 후보에 사용되지 않은 **독립 비율 행**도 검사한다. 단어가 포함된 관리·마케팅 문장은 이번 검사 대상이 아니다.
- 상위 부위에 비율만 남아 있어도 하위 부위의 완성 조성을 대신 확정하지 않는다.
- 첫 수정 검사에서 `면 100% / 안감 / 20%`가 거절되는 부작용을 발견했다. 마지막 부위 마커 뒤에 명시 비율이 있으면 앞 조성을 가져오지 않도록 후행 마커 판정을 보완했다.
- 100% 합계 검증, 소재 사전, OCR 호출 정책은 유지한다. 실패 29건의 인식률 개선은 이번 분석 다음 단계로 남긴다.

## 검증 결과

- 수정 전 신규 오답 방지 사례 5개가 실패하여 문제를 재현했다.
- 수정·부작용 보완 후 파서 관련 122개 통과. 전체 AI·QA **298개 통과**, Ruff 통과.
- 전체 검사는 처음에 임시 상위 폴더 누락으로 setup 오류가 발생했다. 전용 폴더 생성 후 재실행하여 통과했다.
- `tests/test_backend_ai_wiring.py`는 로컬 DB import 부작용 때문에 제외했다. 백엔드·프론트엔드 연동은 검증하지 않았다.
- 실제 이미지 89건을 `scripts.run_qa_batch --offline`으로 재검증: 캐시 hit/miss/write **135/0/0**, API 사용 이미지 **0**, 예외 **0**.
- 원래 정답지 기준 일치 **58/89(65.2%) 유지**. 기존 성공 정답 58건의 회귀 0건. 상태·오류 코드·소재·대표 부위가 바뀐 것은 QA031 한 건이다.
- 파서 성공 60 → 59, 인식 실패 29 → 30. 성공 불일치 2 → 1이며 남은 불일치는 QA003 정답지 오류다. 안전 거절을 개선한 것이며 인식률 향상은 아니다.
- 모든 저장된 원본·기본 전처리 텍스트 후보를 별도로 점수 비교해도 소재·성공 상태가 바뀐 것은 QA031뿐이다.
- 원본 캐시·정답지·로컬 DB의 검사 전후 SHA-256이 동일하다. 결과 CSV·요약은 루트 `.test-artifacts/qa-failure-audit-20260923/after_results.*`에 별도로 저장했다.

AI 폴더에서의 재현 명령(로컬 자료가 필요함):

```powershell
$env:KDPP_ENABLE_REFLECTION_OCR = "0"
.venv/Scripts/python.exe -m scripts.run_qa_batch --image-dir ../../QA_DATASET/real_qa_163/images --answer-key ../../.test-artifacts/vision-qa-20260923/real89_answer_key.csv --ocr-cache ../../.test-artifacts/vision-qa-20260923/new_vision_cache.json --offline --output ../../.test-artifacts/qa-failure-audit-replay/results.csv
```

비교 입력 SHA-256:

- 캐시: `5eee11a99ee51d0aa171e8ca9b8b4f1afaa42083986e3292d27c4aeef1caae37`
- 정답지: `e146faa43c67b8d3a82ce6a5c34a1db48ed52a403cb71b24b87dc3d629ad4c59`

## 다음 작업

1. QA001·QA003의 사진/정답 연결과 대표 부위 정답을 재주석한다. 원본 자료 변경은 별도 작업으로 한다.
2. 파서 해석 11건은 PIMACOTTON 별칭 5건, 비율 선행 배치 2건, 다국어 반복 4건으로 나눠 개선한다. 미등록 소재·미완성 부위 방어를 약화하지 않도록 각각 회귀 검사한다.
3. 복합 2건과 OCR 근거 부족 15건은 원본 사진·부위 배치 검토 후 전처리 개선 대상을 정한다. 반사 후보의 QA060 개선은 이미 저장된 후보에서만 확인한 결과다.
4. 정답지 오류가 적어도 2건 있으므로 기존 65.2%를 검수 완료된 실사진 정확도로 보고하지 않는다. 새 Vision 측정은 하지 않았다.
